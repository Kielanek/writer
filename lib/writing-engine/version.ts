/**
 * Bumped whenever the composition logic (rule hierarchy, section ordering,
 * or how rules are merged) changes in a way worth tracking against existing
 * Documents. This is for debugging/inspection only — it does not drive any
 * migration behavior. See lib/writing-engine/composePrompt.ts.
 */
export const WRITING_ENGINE_VERSION = 1;
