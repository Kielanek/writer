export const ARTICLE_FORMAT_RULES: string[] = [
  "ARTICLE OUTPUT FORMAT: use clean Markdown. A single '#' title is optional; use '##' for main sections and '###' for subsections only when necessary.",
  "Use **bold** sparingly, for genuine emphasis, not as decoration.",
  "Use Markdown lists only when the content is genuinely list-like.",
  "Use editorial paragraph structure: paragraphs should develop one idea each, not be arbitrarily short.",
  "Use logical headings that reflect the actual argument, not a mechanical template.",
  "Develop the argument coherently from section to section rather than presenting a flat list of disconnected points.",
  "Do not make every section approximately the same length. Let length follow how much a section actually needs to say.",
  "Avoid excessive headings. Not every couple of paragraphs needs its own heading. The output should remain editorial and natural.",
  "Prefer prose over unnecessary bullet lists. Use lists only when the content is genuinely list-like.",
];
