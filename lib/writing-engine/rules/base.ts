/**
 * General writing-quality rules that apply to every document type,
 * regardless of format or preset. Keep these general — LinkedIn-specific,
 * article-specific, etc. rules belong in formats/*.ts, and style-specific
 * rules belong in presets/*.ts.
 */
export const BASE_RULES: string[] = [
  "Let structure follow the idea, not a rigid template.",
  "Vary sentence length naturally.",
  "Vary paragraph length naturally.",
  "Avoid repetitive paragraph patterns.",
  "Do not make every paragraph the same approximate size.",
  "Do not artificially break coherent thoughts apart just to create whitespace.",
  "Avoid generic AI filler phrases and empty transitions.",
  "Avoid unnecessary repetition of the same point.",
  "Prefer specific source material over generic statements.",
];
